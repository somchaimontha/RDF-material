# แผนการพัฒนา — ระบบบริหารจัดการครุภัณฑ์ มูลนิธิ RDF

อัปเดตล่าสุด: 14 ส.ค. 2569
เอกสารนี้เป็นแผนรวมสำหรับพัฒนาระบบต่อจากเวอร์ชันปัจจุบัน (deploy code.gs v23)
อ้างอิงบันทึกการทำงาน: `WORK_LOG.md`

## หลักการ / ขอบเขต

- งานส่วนใหญ่ทำฝั่ง `code.gs` (backend/API) + `index.html` (frontend)
- **งด 2FA** ตามสั่ง — เสริมด้วย **การบังคับรหัสผ่านให้รัดกุม** แทน
- ทุก feature ต้องมีระดับสิทธิ์ถูกต้อง (staff/admin) และไม่ลดความปลอดภัยเดิม
- หลังแก้ `code.gs` ต้องให้ผู้ใช้ deploy ใหม่เสมอ (เวอร์ชันเพิ่ม)

---

## ระยะที่ 1 — เสริมความปลอดภัย + นำเข้าข้อมูลชุดใหญ่ (เริ่มทำทันที)

### P1.1 บังคับรหัสผ่านให้รัดกุม
- เกณฑ์ใหม่: อย่างน้อย 10 ตัว, มีตัวพิมพ์เล็ก, ตัวพิมพ์ใหญ่, ตัวเลข และอักขระพิเศษ
- นำไปใช้ทุกจุดที่ตั้งรหัสผ่าน: สร้างบัญชี, เปลี่ยนรหัสผ่านเอง, แก้ไขข้อมูลส่วนตัว
- แสดงคำแนะนำ (hint) เกณฑ์รหัสผ่านในหน้าเว็บ
- ✅ จัดการในระยะนี้

### P1.2 นำเข้าข้อมูลชุดใหญ่ (Bulk import)
- backend: `addBulkAssets` รับอาร์เรย์รายการ → ตรวจ/ทำความสะอาด → ใส่ทีละชุด (setValues)
  - สร้างรหัสครุภัณฑ์ + QR ให้อัตโนมัติต่อชิ้น (แบบเดียวกับตอนเพิ่มทีละชิ้น)
  - จำกัดครั้งละ 2,000 รายการ, ตรวจหมวดหมู่/ราคา/จำนวน/ความยาวทุกช่อง
- frontend: ปุ่ม "นำเข้าข้อมูล" ในหน้าหลัก → modal วาง/พิมพ์หลายแถว (ชื่อ, หมวดหมู่, จำนวน,
  ราคา, แหล่งเงิน, สถานที่, วันที่ซื้อ, ...) → สรุปผลกี่รายการ
- ✅ จัดการในระยะนี้

### P1.3 ประวัติการเข้าสู่ระบบ (Login history)
- backend: ชีต `LoginHistory` (timestamp, username, result, detail) + บันทึกทุกครั้งที่ login
  สำเร็จ/ล้มเหลว + API `getLoginHistory` (admin)
- frontend: แท็บ "เข้าสู่ระบบ" ในเมนู รายงาน (admin) ดูได้
- ✅ จัดการในระยะนี้

---

## ระยะที่ 2 — การจัดการยืม-คืน + อายุการใช้งาน

### P2.1 ยืม-คืนแบบแยกจำนวนบางส่วน (quantity > 1)
- รองรับของที่มีหลายชิ้น: ยืมได้แค่บางจำนวน คืนได้บางส่วน คงเหลือ/ยืมแยกตามจำนวน
- ปรับ schema/บันทึกให้แสดงจำนวนคงเหลือจริง + ประวัติ

### P2.2 ประวัติซ่อมแซม / บำรุงรักษาต่อชิ้น
- บันทึกการซ่อม: วันที่, รายการที่ทำ, ค่าใช้จ่าย, ผู้รับผิดชอบ — ดูในหน้ารายละเอียดครุภัณฑ์

### P2.3 วันหมดประกัน / อายุการใช้งาน
- เพิ่มช่องวันหมดประกัน/กำหนดอายุใช้งาน + แจ้งเตือนล่วงหน้า (ต่อยอดจากระบบแจ้งเกินกำหนดคืน)

---

## ระยะที่ 3 — การบริหารครุภัณฑ์รอบด้าน

### P3.1 การตรวจนับประจำปี (Stocktake)
- ระบบสร้างรายการตรวจนับ, ตรวจว่า ครบ/ขาด/เกิน จากรายการที่คาดไว้ vs นับจริง

### P3.2 ค่าเสื่อมราคา / มูลค่าปัจจุบัน
- คำนวณจากราคา + วันที่ซื้อ + อายุการใช้งาน แสดงมูลค่าคงเหลือ

### P3.3 ใบโอนย้าย / เปลี่ยนสถานที่
- บันทึกประวัติการย้ายจากห้องหนึ่งไปอีกห้อง + แสดงในประวัติ

### P3.4 เวิร์กโฟลว์จำหน่าย (Disposal)
- จากสถานะ "จำหน่ายแล้ว" ขยายเป็นขั้นตอน: ขอจำหน่าย → อนุมัติ → บันทึกเหตุผล/ผู้รับรอง

---

## ระยะที่ 4 — บทบาท + UX + ระบบแจ้งเตือน

### P4.1 บทบาทเพิ่มเติม "ผู้ตรวจสอบ"
- รองรับบทบาทที่สาม ใช้ลงชื่อเอกสารรายงานได้ (ต่อจากเมนู "รายงานเอกสาร")

### P4.2 แจ้งเตือนในเว็บ (badge)
- เช่น มีของเกินกำหนดคืน/หมดประกัน → แสดง badge ในแถบเมนูโดยไม่ต้องเปิดอีเมล

### P4.3 การสำรองข้อมูล / export
- ปุ่ม export ข้อมูลทั้งระบบเป็นระยะ + คำแนะนำการสำรอง

---

## สถานะ

- [x] ระยะที่ 1 (P1.1–P1.3) — ✅ เสร็จแล้ว (P1.1 รหัสรัดกุม, P1.2 Bulk import, P1.3 Login history)
- [ ] ระยะที่ 2
- [ ] ระยะที่ 3
- [ ] ระยะที่ 4

## หมายเหตุ
- ทุกครั้งที่แก้ `code.gs` ต้องแจ้งผู้ใช้เพื่อ deploy (เพิ่มเวอร์ชัน) เสมอ
