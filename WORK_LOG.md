# บันทึกการทำงาน — ระบบครุภัณฑ์ หอพัก RDF วก

อัปเดตล่าสุด: 29 ส.ค. 2569

## ภาพรวมระบบ

Google Apps Script (backend/API) + Google Sheets (ฐานข้อมูล) + Google Drive (เก็บรูป/ไฟล์แนบ)
สำหรับติดตามครุภัณฑ์หอพัก — เพิ่มรายการ, ติดสถานะ, ยืม-คืน, QR code ต่อชิ้น,
ประวัติการดำเนินการ, ระบบ login แยกรายบัญชี, บันทึกข้อผิดพลาด

**สถาปัตยกรรมปัจจุบัน:**

```
GitHub Pages (index.html)  --fetch()-->  Apps Script /exec (JSON API)  --> Google Sheets / Drive
                                                                        --> CacheService (session)
```

- `index.html` = หน้าเว็บ (frontend) ล้วนๆ ไม่มี logic เชื่อม Sheet โดยตรง — โฮสต์ที่
  `https://somchaimontha.github.io/RDF-material/`
- `code.gs` = backend เดียวที่คุยกับ Sheet/Drive ได้ **ห้ามอัปโหลดขึ้น GitHub เด็ดขาด**
  (มี `SPREADSHEET_ID`, `DRIVE_FOLDER_ID` อยู่ในนั้น)
- เชื่อมกันด้วย URL เดียว: `API_URL` ใน `index.html` ต้องตรงกับ URL
  `.../exec` ของ Apps Script deployment เสมอ (ปัจจุบัน v23)

## ไฟล์ในโปรเจกต์

| ไฟล์ | หน้าที่ | อัปโหลดขึ้น GitHub ได้ไหม |
|---|---|---|
| `code.gs` | Backend / JSON API, คุย Sheets+Drive+session+auth | **ห้าม** |
| `appsscript.json` | Config ของ Apps Script | **ห้าม** |
| `index.html` | Frontend ทั้งหมด | ได้ (อัปโหลดเฉพาะไฟล์นี้) |
| `.gitignore` | กัน code.gs/appsscript.json หลุดขึ้น git โดยไม่ตั้งใจ | - |

## สิ่งที่ทำไปแล้ว

1. **แก้ `code.gs` ที่พังทั้งไฟล์** — ต้นฉบับขาดเครื่องหมายคำพูด/`//`/`||`/`<`/`?`/`:`
   ทั่วทั้งไฟล์ ทำให้รันไม่ได้เลย แก้ให้ผ่าน `node --check` แล้ว
2. **เติมช่องที่ backend รองรับแต่ frontend เปิดไม่ครบ** — ผู้ติดตั้ง, ใบเสร็จ 3-4,
   ตัวกรองหมวดหมู่ที่ขาด
3. **ระบบพิมพ์ป้าย QR** — พิมพ์ทีละชิ้น/หลายชิ้น พร้อมย่อฟอนต์อัตโนมัติไม่ให้ข้อความ
   ตัดขาด, ดาวน์โหลด PNG จริง, ใช้ "Save as PDF" ในหน้าต่างพิมพ์เพื่อส่งออกไฟล์,
   หน้าต่างดูตัวอย่างด่วน (quick preview)
4. **ฟีเจอร์ยืม-คืน**: สถานะ "ถูกยืมอยู่", วันครบกำหนด, แจ้งเกินกำหนด, แก้ไข/ลบครุภัณฑ์
   แบบ soft-delete, สรุปสถิติ + กราฟหมวดหมู่หน้า Dashboard
5. **ความปลอดภัยฝั่ง backend**:
   - กันสูตร Google Sheets แฝง (formula/CSV injection) ทุกช่องข้อความอิสระ (`sanitizeForSheet`)
   - ป้องกันแก้แถวหัวตาราง (`assertValidRowIndex`/`assertRowMatches`)
   - ตรวจชนิด/ขอบเขตข้อมูลทุกช่อง (category/status whitelist, ตัวเลขไม่ติดลบ, จำกัดความยาวข้อความ)
   - รหัสผ่านแฮชแบบ SHA-256 (`sha256:` prefix), เทียบแบบ constant-time กัน timing attack
   - ล็อกบัญชีชั่วคราวเมื่อ login ผิดเกินกำหนด (กัน brute-force, คิดตาม username)
6. **ดีไซน์หน้า Dashboard** — ปรับใหม่ตาม Flip7 design system (โทนเขียวมิ้นท์-ทอง-คอรัล-ฟ้า),
   เปลี่ยนรายการจากตารางเป็นการ์ดพร้อมรูปภาพย่อของครุภัณฑ์, stat tile/หมวดหมู่คลิกกรองข้อมูลได้,
   เมนูล่าง (bottom nav) เมื่อบีบหน้าจอมือถือ (ลอกแบบจาก rdf-billing-app)
7. **ย้ายสถาปัตยกรรมเป็น GitHub Pages + Apps Script API** — `code.gs` เปลี่ยนจากเสิร์ฟ HTML
   เป็น JSON API ล้วน (`doGet`/`doPost` ผ่าน `API_FUNCTIONS` dispatcher),
   `index.html` เปลี่ยนจาก `google.script.run` เป็น `fetch()` เรียก API ข้าม origin
   (POST เป็น `text/plain` เพื่อเลี่ยง CORS preflight ที่ Apps Script ตอบไม่ได้)
8. **ระบบ login แยกรายบัญชี (แทน PIN กลาง)** — อ้างอิงแบบจาก `D:\RDF_001` และ
   `rdf-billing-app`:
   - ชีต `Staff` (`username, password, displayName, email, role, status, createdAt`)
   - 2 สิทธิ์: `staff` (เพิ่ม/แก้ไข/ยืม-คืน) และ `admin` (เพิ่มสิทธิ์ลบ, จัดการบัญชี,
     ฟังก์ชันบำรุงรักษา)
   - session token (UUID) เก็บใน `CacheService` อายุ 5 ชั่วโมง
   - ทุกฟังก์ชันที่แก้ข้อมูลบันทึก "ใครทำ" จาก session จริง ไม่ใช่ช่องกรอกอิสระอีกต่อไป
   - ผู้ใช้แก้ไขข้อมูลส่วนตัว (ชื่อ/อีเมล) และเปลี่ยนรหัสผ่านเองได้
   - admin สร้าง/ปิดใช้งานบัญชีพนักงานได้ (ไม่มีการลบถาวร เพื่อให้ประวัติยังอ้างอิงได้)
9. **Google Sign-In** — ล็อกอินด้วย Gmail เป็นทางเลือกของ username+password
   - ตรวจ ID token ผ่าน `https://oauth2.googleapis.com/tokeninfo` พร้อมเช็ค `aud`
     (audience) และ `email_verified` ก่อนอนุญาต (เข้มกว่าตัวอย่างต้นแบบที่ไม่เช็ค `aud`)
   - จับคู่กับอีเมลที่ผูกไว้ในชีต `Staff` (คอลัมน์ `email`)
   - Client ID เก็บใน Script Properties คีย์ `GOOGLE_CLIENT_ID` (ตั้งผ่าน Apps Script
     Project Settings → Script properties, หรือผ่านหน้าเว็บ "จัดการบัญชี" ก็ได้)
   - ปุ่ม/ข้อความของ Google แสดงเป็นภาษาไทย (`hl=th`, `locale: 'th'`)
10. **สแกน QR แล้วดูรูปได้ทันที** — หน้ารายละเอียดครุภัณฑ์ (`action=viewItem&id=...`)
    แสดงรูป `image/image2/receipt/receipt2/receipt3/receipt4` ถ้ามี ไม่มีก็ไม่แสดง
    ไม่ต้อง login และคลิกรูปเปิดเป็น popup (lightbox) แทนเปิดแท็บใหม่
14. **ล็อกหน้ารายการหลัก (dashboard) ไว้หลัง login** — `getAssets` (รายการทั้งหมด/สรุป
    มูลค่า) ต้อง login ก่อนถึงจะดูได้แล้ว (เดิมเปิดสาธารณะ) ส่วน `getAssetById`
    (หน้ารายละเอียดชิ้นเดียวจากการสแกน QR) ยังคงดูได้โดยไม่ต้อง login เหมือนเดิม —
    หน้าเว็บมีปุ่ม "เข้าสู่ระบบ" ให้เห็นชัดเจนที่แถบเมนู และมีหน้าจอ "ต้องเข้าสู่ระบบ"
    แทนรายการเมื่อยังไม่ได้ login
15. **แก้บั๊ก QR แบบเปล่าไม่มีรายละเอียด** — เดิม "เปิดรูปเต็ม"/"ดาวน์โหลด PNG" ได้แค่รูป
    QR ล้วนๆ (เสี่ยงสับสนกับ QR ของชิ้นอื่น) ตอนนี้ประกอบชื่อ+รหัสครุภัณฑ์ลงในรูปด้วย
    ทุกครั้งที่เปิด/ดาวน์โหลด
16. **แก้ป๊อปอัปดูรายละเอียดครุภัณฑ์ซ้อนทับกับแถบเมนูด้านบน** (z-index ผิด)
17. **เมนู "ตั้งค่า" (admin) + จัดการ "แหล่งเงิน" แบบไม่ต้องแก้โค้ด** — เพิ่มชีต
    `FundingSources` (name, status, createdAt) เก็บรายการแหล่งเงินที่เลือกได้ตอน
    เพิ่ม/แก้ไขครุภัณฑ์ — admin เพิ่ม/ปิดใช้งานรายการได้เองจากเมนู "ตั้งค่า" บนเว็บ
    โดยไม่ต้องแตะ `code.gs` อีกต่อไป (ครั้งนี้เป็นการวางโครงระบบครั้งเดียว)
    ตอนสร้างชีตครั้งแรก ระบบจะดึงค่าที่เคยกรอกไว้ในข้อมูลเดิมมาใส่เป็นตัวเลือกให้อัตโนมัติ
    — ย้าย "ตั้งค่า Google Sign-In" มาไว้ในเมนูนี้ด้วย (เดิมอยู่ใน "จัดการบัญชี")
18. **เพิ่มช่อง "คำอธิบาย/รายละเอียด"** ให้ครุภัณฑ์แต่ละชิ้น (แยกจาก "หมายเหตุ") —
    กรอกได้ตอนเพิ่ม/แก้ไข และแสดงในหน้ารายละเอียด (ถ้ามีข้อมูล)
19. **สลับมุมมองรายการ การ์ด/ตาราง** ที่หน้าแรก (จำค่าไว้ในเครื่อง)
20. **"แหล่งเงิน" มีตัวเลือก "อื่นๆ (ระบุเอง)"** — พิมพ์แหล่งเงินเฉพาะกิจได้ทันทีโดยไม่ต้อง
    รอ admin เพิ่มเข้าลิสต์ก่อน
21. **คลิก "มูลค่ารวม" ดูรายละเอียดได้** — สรุปมูลค่าแยกตามหมวดหมู่/สถานะ/แหล่งเงิน
    คลิกแต่ละแถวเพื่อกรองรายการที่ตรงกันได้เลย (ทำงานฝั่ง frontend ล้วน ไม่มี API เพิ่ม)
22. **ตัวแสดงสถานะการเชื่อมต่อฐานข้อมูล** ที่แถบเมนูบน (จุดเขียว/แดง) — อิงจากผลการเรียก
    API จริงที่เกิดขึ้นอยู่แล้ว ไม่ได้ยิง request พิเศษเพิ่มไปเช็คเฉยๆ (กันโหลด Apps Script เพิ่ม)
23. **เมนู "รายงาน" ใหม่ (admin)** — 2 แท็บ: "ประวัติการดำเนินการ" (audit trail รวมทุก
    ชิ้น ไม่ใช่แค่ทีละชิ้นเหมือนเดิม) และ "บันทึกข้อผิดพลาด" (ErrorLog ในเว็บ ไม่ต้องเปิด
    Sheet ดูตรงๆ อีกต่อไป)
24. **แจ้งเตือนของเกินกำหนดคืนทางอีเมล** — ฟังก์ชัน `checkOverdueAndNotify` ส่งอีเมล
    สรุปให้ admin ทุกคนที่ผูกอีเมลไว้ กดปุ่ม "ส่งแจ้งเตือนตอนนี้" ในเมนู "ตั้งค่า" ได้เลย
    หรือจะตั้ง time-driven trigger ให้รันอัตโนมัติทุกวันก็ได้ (ตั้งครั้งเดียวที่ Apps Script
    editor → ไอคอนนาฬิกา → Add Trigger → เลือกฟังก์ชันนี้ → Day timer)

25. **ส่งออกรายงาน CSV/PDF** — ปุ่ม "CSV" (ดาวน์โหลดเปิดด้วย Excel ได้ ใส่ BOM กันภาษาไทยเพี้ยน)
    และ "PDF" (พิมพ์รายงานสรุป ใช้ "บันทึกเป็น PDF" ในหน้าต่างพิมพ์ — ใช้กลไกเดียวกับ
    การพิมพ์ป้าย QR) ส่งออกตามรายการที่กรอง/ค้นหาอยู่ ณ ขณะนั้น (frontend ล้วน)
26. **เมนู "รายงานเอกสาร" (admin)** — สร้างเอกสารรายงานแบบทางการ (A4) อิงแบบจาก
    `rdf-billing-app` (Export Report) และ `RDF_001` (`reports.html`):
    - ตั้งค่าหัวเอกสาร: ชื่อหน่วยงาน, หัวข้อ, เลขที่เอกสาร, วันที่เอกสาร, ช่วงวันที่ (กรองจาก "วันที่ซื้อ")
    - สรุปตาม หมวดหมู่ / สถานะ / แหล่งเงิน (เลือกได้) + ตารางรายการละเอียด + บล็อกการลงชื่อ
      (ผู้จัดทำ / ผู้ตรวจสอบ / ผู้อนุมัติ) — มี live preview ฝั่งขวา
    - ส่งออกได้ 3 แบบ: **PDF** (ผ่าน #printArea + window.print → "บันทึกเป็น PDF"),
      **CSV** (BOM กันภาษาไทยเพี้ยน), **Excel** (HTML-table .xls ไม่ต้องพึ่ง SheetJS)
    - ทำงานฝั่ง frontend ล้วนจาก `allAssets` ที่โหลดมาแล้ว ไม่มีการเรียก API เพิ่ม
    - เข้าถึงผ่านปุ่ม "รายงานเอกสาร" ในแถบเมนูบน (admin เท่านั้น)
27. **บังคับรหัสผ่านให้รัดกุม** — เกณฑ์ใหม่: อย่างน้อย 10 ตัว + ตัวพิมพ์เล็ก/พิมพ์ใหญ่/ตัวเลข/
    อักขระพิเศษ (`validatePasswordPolicy`) ใช้ทุกจุดที่ตั้งรหัสผ่าน (สร้างบัญชี, เปลี่ยนรหัสผ่าน)
    + อัปเดตคำแนะนำในหน้าเว็บ (งด 2FA ตามสั่ง ใช้รหัสรัดกุมแทน)
28. **นำเข้าข้อมูลชุดใหญ่ (Bulk import)** — backend `addBulkAssets` รับอาร์เรย์รายการ
    ตรวจ/ทำความสะอาดทุกช่อง (หมวดหมู่ whitelist, ราคา/จำนวน, จำกัดความยาว), สร้างรหัสครุภัณฑ์+QR
    อัตโนมัติไม่ชนซ้ำ (`generateBulkInventoryIds`/`buildAssetRowValues`), ใส่ทีละชุดด้วย setValues
    (ครั้งละ ≤2,000), ป้องกันรหัสซ้ำชุด/ซ้ำกับข้อมูลเดิม — frontend ปุ่ม "นำเข้าข้อมูลชุดใหญ่"
    ในหน้าเพิ่มครุภัณฑ์: modal วางข้อมูลทีละแถว (Tab/;/,) + ตัวอย่าง + นับจำนวน/แจ้งข้อผิดพลาดก่อนส่ง
29. **ประวัติการเข้าสู่ระบบ (Login history)** — ชีต `LoginHistory` + `logLogin` บันทึกทุกครั้งที่
    login สำเร็จ/ล้มเหลว (ไม่เคยบล็อกการ login เอง) + API `getLoginHistory` (admin) —
    เพิ่มแท็บ "เข้าสู่ระบบ" ในเมนู รายงาน (admin) ดูได้
30. **แก้ความเสี่ยงจากการวิเคราะห์โค้ด (Security/bug hardening)** —
    - ปิดช่องทางเรียก maintenance API โดยไม่มี admin token: `setup`, `regenerateAllQrCodes`,
      `fixDuplicateInventoryIds`, `checkOverdueAndNotify` ยังรันตรงจาก Apps Script editor/trigger
      ได้เหมือนเดิม แต่ถ้าเรียกผ่านเว็บ/API ต้องเป็น admin
    - เพิ่ม `escapeHtml`/`jsString` ฝั่ง `index.html` แล้วใช้กับรายการครุภัณฑ์, รายงาน,
      ประวัติ, รายชื่อบัญชี, แหล่งเงิน, QR preview/print และ popup รายละเอียด เพื่อลดความเสี่ยง XSS
      จากข้อมูลที่มาจาก Sheet/API
    - ปรับ `getAssetById` ให้ผู้ไม่ login เห็นเฉพาะข้อมูลพื้นฐานจาก QR ส่วนประวัติ/ใบเสร็จ/ข้อมูลเต็ม
      จะแสดงเมื่อมี session ถูกต้องเท่านั้น โดย frontend ส่ง token ผ่าน POST body ไม่ใส่ใน query string
    - แก้การสร้าง `inventoryId` จากเดิมนับจำนวนรายการเป็นหาเลขลำดับสูงสุด (`max + 1`),
      เพิ่มการเช็ก duplicate ก่อนบันทึก และใช้ `LockService` ครอบการเพิ่มรายการเดี่ยว/นำเข้าชุดใหญ่
      เพื่อลดโอกาสรหัสชนกันเมื่อหลายคนบันทึกพร้อมกัน
    - เพิ่มการตรวจไฟล์อัปโหลด: รับเฉพาะ JPEG/PNG/WebP/PDF และจำกัดขนาดประมาณ 10 MB ต่อไฟล์
    - แก้ Bulk import parser ให้ตรงกับข้อความตัวอย่างบนหน้าเว็บ:
      `ชื่อรายการ, หมวดหมู่, จำนวน, ราคา, แหล่งเงิน, สถานที่, วันที่ซื้อ, ผู้ซื้อ, ผู้ติดตั้ง, หมายเหตุ`
31. **ปรับ UI หน้า Dashboard เป็นแบบ sidebar workspace** —
    - เพิ่ม sidebar สีเข้มแบบ AssetQR พร้อมเมนู: แดชบอร์ด, สแกน QR, ทะเบียนครุภัณฑ์,
      กำหนดตรวจ, พิมพ์ QR/การ์ด, ประวัติการตรวจ, งานแจ้งซ่อม, ข้อมูลตั้งต้น, รายงาน, ผู้ใช้งาน
    - ปรับ topbar ให้มีช่องค้นหากลางหน้า และ sync กับช่องค้นหารายการเดิม
    - จัด dashboard ใหม่เป็นการ์ด KPI 4 ใบ, donut chart แยกสถานะ, แถบแยกหมวดหมู่,
      กล่องค่าใช้จ่ายงานซ่อม, รายการต้องตรวจเร็ว ๆ นี้ และงานซ่อมที่ยังไม่เสร็จ
    - เมนูใหม่ใช้ข้อมูลและฟังก์ชันเดิมทั้งหมด: filter รายการ, เปิด popup รายละเอียด,
      ตั้งค่าป้าย QR, รายงาน, ผู้ใช้งาน และตั้งค่าระบบ โดยไม่เปลี่ยน backend/API
    - เมนูข้อมูลตั้งต้นที่ยังไม่มี backend เฉพาะทางจะแสดงเป็นจุดต่อยอด UI หรือพาไปตัวกรองเดิม
      เพื่อไม่ทำให้การทำงานเดิมเสีย

## ยังไม่ได้ทำ (ผู้ใช้ขอเพิ่มเติม รอทำต่อ)
- นำเข้าข้อมูลชุดใหญ่ (Bulk import)
- ยืม-คืนแบบแยกจำนวนบางส่วน (quantity > 1)
11. **คลิกดูรายละเอียดจาก Dashboard เป็น popup ในหน้าเดียวกัน** แทนการเปิดแท็บใหม่
12. **แก้บั๊กรหัสครุภัณฑ์ (`inventoryId`)**:
    - แก้บั๊ก "falsy zero" ใน `regenerateAllQrCodes` ที่ทำให้รหัส `"0"` ถูกข้ามไม่สร้าง QR
    - เพิ่ม `fixDuplicateInventoryIds` ตรวจรหัสซ้ำ สร้างรหัสใหม่ให้อัตโนมัติ พร้อม
      สร้าง QR ใหม่, บันทึกประวัติการเปลี่ยน, ส่งอีเมลสรุปให้ผู้ดูแล
13. **ระบบบันทึกข้อผิดพลาด (`ErrorLog`)** — ทั้ง error ฝั่ง client (เช่น
    "Unexpected token '<'" ที่เกิดจาก Apps Script ตอบ HTML แทน JSON เป็นครั้งคราว)
    และฝั่ง server จะถูกบันทึกลงชีต `ErrorLog` (ตัด password/token ออกก่อนบันทึกเสมอ)
    — ฝั่ง frontend มี retry อัตโนมัติ (สูงสุด 3 ครั้ง) ก่อนจะแจ้ง error ให้ผู้ใช้

## สถานะการ deploy (ล่าสุด)

- ✅ `code.gs` deploy แล้ว เวอร์ชัน 29 (29 ส.ค. 2569 13:50) — อัปเดต security hardening,
  Bulk import, Login history และ Dashboard UI endpoint ผ่าน deployment เดิม
- ✅ `index.html` push ขึ้น GitHub Pages แล้ว (ล่าสุด: ปรับ Dashboard เป็น sidebar workspace +
  บันทึก security hardening)
- ✅ รัน `setup`, `regenerateAllQrCodes`, `fixDuplicateInventoryIds` แล้ว — **⚠️ หลัง deploy ต้องรัน
  `setup()` อีกครั้ง (เวอร์ชันใหม่) เพื่อสร้างชีต `LoginHistory`**

## เช็กลิสต์ที่เหลือ (Google Sign-In)

- [ ] ยืนยัน Script Properties มีคีย์ `GOOGLE_CLIENT_ID` ค่า
      `692943413718-823rsampc4jafpf897m89iv7v13nrf10.apps.googleusercontent.com`
- [ ] ที่ Google Cloud Console → OAuth Client ID → "Authorized JavaScript origins"
      ต้องมี `https://somchaimontha.github.io`
- [ ] ผูกอีเมลให้บัญชี admin ที่ใช้งานอยู่ (ผ่าน "แก้ไขข้อมูลส่วนตัว" หรือแก้ตรงคอลัมน์
      `email` ในชีต `Staff`)
- [ ] ทดสอบกดปุ่ม Google Sign-In บนเว็บจริงว่าล็อกอินเข้าได้

## จุดที่รู้อยู่แล้วว่ายังไม่สมบูรณ์ (ยังไม่ได้ทำ)

- ยืม-คืนแบบแยกจำนวนบางส่วน (ของที่มี quantity > 1) — คุยออกแบบไว้แล้วแต่ยังไม่ได้ลงมือทำ
- ไม่มีรายงาน/ส่งออก Excel, ไม่มีนำเข้าข้อมูลชุดใหญ่
- ยังไม่มีหน้าดู `ErrorLog` ในเว็บ (ต้องเปิดชีตดูตรงๆ ก่อน)

---

## ⚠️ เรื่องสำคัญ: การ commit ภาษาไทยพัง (mojibake) — แก้และกันไว้แล้ว

**สาเหตุ:** Windows console เป็น codepage `874` (ไทย) → ถ้าใช้ `git commit -m "ภาษาไทย..."`
จาก PowerShell ตรงๆ ตัวข้อความจะถูกส่งเป็น windows-874 → git เก็บ byte นั้น → commit กลายเป็น
`เธฃเธงเธก...` (อ่านไม่ออก) บน GitHub ทั้งๆ ที่โค้ด/ไฟล์ปกติ 100%

**สิ่งที่ตั้งค่าแล้ว (ใน repo นี้):**
```bash
git config i18n.commitEncoding utf-8
git config i18n.logOutputEncoding utf-8
git config core.quotepath false
```

**วิธี commit ที่กันพังได้จริง (ใช้ได้ 100%):** เขียนข้อความลงไฟล์ **UTF-8** แล้ว
`git commit -F <ไฟล์>` (git อ่าน byte ในไฟล์ตรงๆ ไม่ผ่าน console)

**เครื่องมือในโปรเจกต์:** มีไฟล์ `commit-th.ps1` ใช้ดังนี้
```powershell
.\commit-th.ps1 -FilePath message.txt   # แนะนำ: ข้อความจากไฟล์ UTF-8 (กันพังชัวร์)
.\commit-th.ps1 -Message "สรุป..."       # ใช้ได้ถ้า console แสดงไทยถูก
```
ทดสอบแล้วว่า byte ที่เก็บใน reflog ตรงกับ UTF-8 (`e0 b8 aa e0 b8 a3...` = "สรุป")

### หมายเหตุเพิ่มเติม (กันไฟล์ .ps1 พังเอง)
- สคริปต์ `.ps1` ภาษาไทยที่สร้างแล้วไม่มี BOM PowerShell 5.1 จะอ่านผิดเป็นอักขระ
  → ถ้าจะเขียนสคริปต์ที่มีภาษาไทย ควรหลีกเลี่ยงข้อความไทยในคอนเทนต์หรือแน่ใจว่า encoding ถูก
  - (บัญชีย่อ) เรื่องนี้คือสาเหตุที่ commit ก่อนหน้าโชว์ `เธฃเธง...` มันแค่แสดงผล ส่วนตัวโค้ดไม่กระทบ
## ข้อควรจำ (ตกลงกันไว้)

- คุยและเขียนไฟล์เป็นภาษาไทย/อังกฤษเท่านั้น (กัน UTF-8 พัง)
- ทุกครั้งที่ `code.gs` มีการอัปเดต ต้องแจ้งผู้ใช้ทุกครั้ง
