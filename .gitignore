# Never commit the Apps Script backend to a public repo (GitHub Pages only
# needs index.html) — code.gs and appsscript.json contain SPREADSHEET_ID /
# DRIVE_FOLDER_ID for the real Google Sheet + Drive folder.
code.gs
appsscript.json
.clasp.json

# Local notes file containing admin temp password + a stale GitHub PAT — must
# never leak to a public GitHub Pages repo.
V.3*.txt
